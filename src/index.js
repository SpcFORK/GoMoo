const database = require("./database/index"),
  encode = require("./encode/index"),
  encrypt = require("./encrypt/index"),
  user = require("./user/index");

module.exports = {
  database,
  encode,
  encrypt,
  user,
};
